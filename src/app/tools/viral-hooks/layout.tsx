
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Viral Hook Generator - Free Social Media Tool | Micropost",
    description: "Generate scroll-stopping hooks for your social media posts with AI. Free tool for LinkedIn, Twitter, and Instagram.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
